// Placeholder — overwritten at build time by amplify.yml with real values.
// Local dev uses .env.local instead; these empty strings are never used locally.
export const RUNTIME_CONFIG: Record<string, string> = {
  DATABASE_URL: '',
  OPENAI_API_KEY: '',
  SESSION_SECRET: '',
  RESEND_API_KEY: '',
  RESEND_FROM: '',
  TAVILY_API_KEY: '',
}
